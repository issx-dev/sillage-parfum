# Dependencies
node_modules/
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
yarn.lock

# Next.js
.next/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Misc
tsconfig.tsbuildinfo
*.log

# Debug scripts and outputs (may contain secrets)
debug/

# Dev tools (local scripts, not source)
dev-tools/

# Audit artifacts (dev-only, not source)
.audit/
.playwright-cli/

# Design audit screenshots and stray capture files
# (public/ assets stay — only the root-level design captures are excluded)
*.png
!public/**/*.png
cart_drawer.png
catalog.png
home.png
product_detail.png
raw_chanel.png
search_overlay.png

# Coverage / test artifacts
coverage/
*.lcov

# Runtime data (orders JSON written by webhook handler — not source)
/data/
