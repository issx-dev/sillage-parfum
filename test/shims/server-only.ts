// Stub for Next.js' "server-only" guard so unit tests can import modules
// that use it (e.g. lib/data.ts). The real package throws at module load
// time when imported outside a Next.js server runtime.
export {};
